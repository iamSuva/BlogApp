<!DOCTYPE html>
<html lang="en">
<head>
    <%- include("./partials/head") %>
    <title>update blog</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css">
    <!-- Add your custom stylesheets if needed -->
    <style>
      body {
        font-family: 'Arial', sans-serif;
        background-color: #989ac4bb;
    
      }
      .navbar {
        background-color: rgb(49, 65, 213);
        position: sticky;
        top: 0;
        z-index: 50;
        color: white;
        font-size: 20px;
        font-weight: 600;
      }
      form{
        background-color: antiquewhite;
        border:1px solid gray;
        border-radius: 20px;;
        width:70%;
        padding:10px 8px;
      }
      </style>
<body>
    <%-include("./partials/nav.ejs") %>
    
    <div class="container">

        <form action="/blogs/update-blog/<%=blog._id %>" method="post" enctype="multipart/form-data">
             
            <div class="mb-3">
                <label for="productName" class="form-label">Blog Title</label>
                <input 
                type="text" 
                class="form-control"
                 id="product" name="title"
                  aria-describedby="productName" value="<%=blog.title %>"/>
     
             </div>  
             
            <div class="mb-3">
                <label for="body" class="form-label">Description</label>
                <textarea name="body" class="form-control" id="body" >
                    <%=blog.body%>
                </textarea>
             </div> 
             <div class="mb-3">
                <label for="blogImage" class="form-label">Current Cover Image</label>
                <img src="/<%=blog.coverImageUrl %>" alt="Current Image" width="200">
             </div>   
             <div class="mb-3">
                <label for="coverimage" class="form-label">Cover Iamge</label>
                <input 
                type="file" 
                class="form-control"
                 id="" name="coverimage"
                  aria-describedby="coverimage"/>
    
             </div> 
             <div class="mb-3">
              <button class="btn btn-info">update</button>
             </div>
             <!-- input hidden to store the existing image -->
             <input type="hidden" name="currentImage" value="<%=blog.coverImageUrl %>">
            
        </form>
      </div>
      
      
</body>
<%-include("./partials/script")%>
</html>